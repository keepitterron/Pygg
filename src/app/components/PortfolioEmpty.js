import * as React from 'react';

export default function PortfolioEmpty() {
  return (<div className="coins__empty">
    <h2>Your portfolio is empty</h2>
    <p>Let's add some coins.</p>
  </div>);
}
